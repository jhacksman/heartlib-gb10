# Backend API URL (primary backend for auth, etc.)
VITE_API_URL=http://localhost:8000

# Multiple backend URLs for parallel generation (comma-separated)
# Each backend generates a different version (A, B, etc.) like Suno
# Example for 2 versions: http://localhost:8000,http://localhost:8001
VITE_BACKEND_URLS=http://localhost:8000,http://localhost:8001
